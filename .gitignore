# Employee-specific data - NEVER commit this!

# All employee input directories (except the example template)
inputs/*/
!inputs/employee-name/

# All generated reviews
outputs/*

# Keep directory structure
!outputs/.gitkeep
